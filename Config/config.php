<?php

/* Déclaration des classes et tables des bases se données correspondantes */

// $classN = "NomDeVotreClasseN"; // On définit la classe ici <-------------------------------------
// $tableN = "NomDeVotreTableN"; // On définit la table ici <-------------------------------------

$classProfil = "Profil"; // On définit la classe ici <-------------------------------------
$tableProfil = "profil"; // On définit la table ici <-------------------------------------

$classSecurite = "Securite"; // On définit la classe ici <-------------------------------------
$tableSecurite = "securite"; // On définit la table ici <-------------------------------------

$classAccueil = "Accueil"; // On définit la classe ici <-------------------------------------
$tableAccueil = "accueil"; // On définit la table ici <-------------------------------------

$classEspece = "Espece"; // On définit la classe ici <-------------------------------------
$tableEspece = "espece"; // On définit la table ici <-------------------------------------

$classAnimal = "Animal"; // On définit la classe ici <-------------------------------------
$tableAnimal = "animal"; // On définit la table ici <-------------------------------------

$classMenus = "Menus"; // On définit la classe ici <-------------------------------------
$tableMenus = "menus"; // On définit la table ici <-------------------------------------

$classEnclos = "Enclos"; // On définit la classe ici <-------------------------------------
$tableEnclos = "enclos"; // On définit la table ici <-------------------------------------

$classObjet = "Objet"; // On définit la classe ici <-------------------------------------
$tableObjet = "objet"; // On définit la table ici <-------------------------------------

$classPresent = "Present"; // On définit la classe ici <-------------------------------------
$tablePresent = "present"; // On définit la table ici <-------------------------------------

$classObjet_achete = "Objet_achete"; // On définit la classe ici <-------------------------------------
$tableObjet_achete = "objet_achete"; // On définit la table ici <-------------------------------------

$classObjet_prete = "Objet_prete"; // On définit la classe ici <-------------------------------------
$tableObjet_prete = "objet_prete"; // On définit la table ici <-------------------------------------

$classAmenager = "Amenager"; // On définit la classe ici <-------------------------------------
$tableAmenager = "amenager"; // On définit la table ici <-------------------------------------

$classCohabiter = "Cohabiter"; // On définit la classe ici <-------------------------------------
$tableCohabiter = "cohabiter"; // On définit la table ici <-------------------------------------

$classHabiter = "Habiter"; // On définit la classe ici <-------------------------------------
$tableHabiter = "habiter"; // On définit la table ici <-------------------------------------

$classManger = "Manger"; // On définit la classe ici <-------------------------------------
$tableManger = "manger"; // On définit la table ici <-------------------------------------

$classParent = "Parent"; // On définit la classe ici <-------------------------------------
$tableParent = "parent"; // On définit la table ici <-------------------------------------

$classSpecialiser = "Specialiser"; // On définit la classe ici <-------------------------------------
$tableSpecialiser = "specialiser"; // On définit la table ici <-------------------------------------

$classVivre = "Vivre"; // On définit la classe ici <-------------------------------------
$tableVivre = "vivre"; // On définit la table ici <-------------------------------------

/* Déclaration de variables */



